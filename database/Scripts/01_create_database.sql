-- Ejecutar en PostgreSQL como usuario postgres si prefieres crear la base manualmente.
-- Si usas dotnet ef database update, EF puede crear la base automáticamente según permisos.

CREATE DATABASE piyi_db;
