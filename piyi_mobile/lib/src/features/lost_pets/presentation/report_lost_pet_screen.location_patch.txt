# PATCH RECOMENDADO

En ReportLostPetScreen ya no pedir latitud/longitud manualmente.

Agregar imports:

```dart
import '../../location/presentation/widgets/piyi_use_current_location_button.dart';
import '../../location/presentation/widgets/piyi_current_location_card.dart';
import 'package:geolocator/geolocator.dart';
```

Agregar estado:

```dart
Position? _selectedPosition;
```

Agregar botón:

```dart
PiyiUseCurrentLocationButton(
  onLocation: (position) {
    setState(() {
      _selectedPosition = position;
      _latitudeController.text = position.latitude.toString();
      _longitudeController.text = position.longitude.toString();
    });
    PiyiSnackBar.success(context, 'Ubicación tomada correctamente.');
  },
),
if (_selectedPosition != null) ...[
  const SizedBox(height: PiyiSpacing.sm),
  PiyiCurrentLocationCard(position: _selectedPosition!),
],
```

Al enviar:

```dart
lastSeenLatitude: _selectedPosition?.latitude ?? num.tryParse(_latitudeController.text.trim()),
lastSeenLongitude: _selectedPosition?.longitude ?? num.tryParse(_longitudeController.text.trim()),
```
